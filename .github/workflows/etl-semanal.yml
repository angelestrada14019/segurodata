# Pipeline de datos + reentrenamiento — SeguroData Bogota
#
# ESTADO: DESACTIVADO (el cron semanal sigue comentado)
# Para activar el cron: descomentar el bloque "schedule" de abajo y hacer commit.
# Recomendacion del equipo (10-jul-2026): no activar el cron antes de la
# sustentacion oral. Probar primero varias veces via "Run workflow" manual.
#
# Que hace (cadena completa Bronze -> Silver -> Model -> Supabase):
#   1. src/pipeline.py       -- descarga incremental F3/F5/F6 (Bronze)
#   2. src/transform.py      -- Bronze -> Silver (nunca se llamaba antes; sin
#                                este paso, "activar el cron" no alcanzaba)
#   3. scripts/train_model.py       -- Silver -> Gold -> XGBoost + SHAP,
#                                       split temporal dinamico
#   4. scripts/load_model_artifacts.py -- carga a Supabase, PERO SOLO SI
#      pasa el quality-gate de metricas.json (macro_f1/accuracy_within_1_band
#      contra umbrales minimos) -- si el modelo nuevo es peor, este paso
#      falla y el job se detiene ANTES de tocar Supabase y antes de commitear
#      nada de vuelta al repo.
#
# Los pasos 2-4 se pueden saltar con el input "retrain=false" (solo refresca
# datos crudos, como el workflow original).

on:
  # schedule:
  #   - cron: '0 6 * * 1'   # cada lunes a las 6am UTC (1am Bogota)

  # Permite lanzar manualmente desde GitHub Actions > Run workflow
  workflow_dispatch:
    inputs:
      sources:
        description: 'Fuentes a actualizar (ej: f3 f5 f6). Dejar vacio para las 3 por defecto.'
        required: false
        default: 'f3 f5 f6'
      force:
        description: 'Forzar re-descarga aunque no haya cambios'
        required: false
        default: 'false'
        type: boolean
      retrain:
        description: 'Correr tambien transform+train+load (cadena completa). false = solo ETL de datos crudos.'
        required: false
        default: 'true'
        type: boolean

jobs:
  etl:
    name: Pipeline de datos y reentrenamiento
    runs-on: ubuntu-latest

    steps:
      - name: Checkout del repositorio
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.GITHUB_TOKEN }}

      - name: Configurar Python 3.11
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
          cache: 'pip'

      - name: Instalar dependencias
        run: |
          pip install -r requirements.txt
          pip install supabase>=2.6.0

      - name: Correr pipeline ETL (Bronze)
        env:
          SOCRATA_APP_TOKEN: ${{ secrets.SOCRATA_APP_TOKEN }}
        run: |
          SOURCES="${{ github.event.inputs.sources || 'f3 f5 f6' }}"
          FORCE_FLAG=""
          if [ "${{ github.event.inputs.force }}" = "true" ]; then
            FORCE_FLAG="--force"
          fi
          # F6 (Hurto PN) NO alimenta Silver/el modelo -- no esta en el
          # diccionario STEPS de src/transform.py, es solo contexto de
          # benchmarking nacional para la sustentacion oral. Se corre aparte
          # en modo best-effort: un fallo ahi (ej. falta SOCRATA_APP_TOKEN,
          # que es un secret DISTINTO de SUPABASE_URL/SUPABASE_SERVICE_KEY)
          # nunca debe bloquear transform/train/load, que no dependen de F6.
          REQUIRED_SOURCES=$(echo "$SOURCES" | tr ' ' '\n' | grep -v '^f6$' | tr '\n' ' ')
          if [ -n "$(echo $REQUIRED_SOURCES | tr -d ' ')" ]; then
            python src/pipeline.py --source $REQUIRED_SOURCES $FORCE_FLAG --verbose
          fi
          if echo "$SOURCES" | grep -qw f6; then
            echo "--- F6 (Hurto PN, solo benchmarking) -- best-effort, no bloquea el resto ---"
            python src/pipeline.py --source f6 $FORCE_FLAG --verbose || echo "[AVISO] F6 fallo (no bloqueante) -- revisar SOCRATA_APP_TOKEN si hace falta refrescar benchmarking"
          fi

      - name: Verificar archivos generados
        run: |
          echo "--- Archivos en datos/raw/ ---"
          ls -lh datos/raw/ || echo "(vacio)"
          echo ""
          echo "--- Estado del pipeline ---"
          python src/pipeline.py --status

      - name: Bootstrap fuentes estaticas (F1/F2/F4/F7/F8) para transform.py
        # datos/raw/ esta gitignored -- un checkout fresco de un runner
        # hosteado por GitHub NO trae estos archivos (a diferencia de una
        # maquina local, donde ya existen de una corrida anterior y rara vez
        # cambian). transform.py falla duro si su Bronze de entrada no
        # existe -- sin este paso, "Transformar Bronze -> Silver" siempre
        # fallaria en un runner limpio. pipeline.py ya es incremental via
        # HTTP Last-Modified (F1/F2/F4/F7) o metadata_modified (F8), asi que
        # correr esto en cada run es barato salvo la primera vez.
        if: github.event.inputs.retrain != 'false'
        run: python src/pipeline.py --source f1 f2 f4 f7 f8 --verbose

      - name: Transformar Bronze -> Silver
        if: github.event.inputs.retrain != 'false'
        run: python src/transform.py --verbose

      - name: Reentrenar modelo (Gold + XGBoost + SHAP)
        if: github.event.inputs.retrain != 'false'
        run: python scripts/train_model.py --verbose

      - name: Cargar a Supabase (gateado por quality-check de metricas)
        if: github.event.inputs.retrain != 'false'
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_KEY: ${{ secrets.SUPABASE_SERVICE_KEY }}
        run: python scripts/load_model_artifacts.py

      - name: Commitear metricas actualizadas
        # SOLO metricas.json -- es la UNICA excepcion deliberada dentro de
        # datos/ que se versiona (ver .gitignore, P1.2). Bronze/Silver/Gold
        # (datos/raw|procesados|features/*) siguen gitignored a proposito
        # (filosofia FTI del proyecto: "los datos no se suben al repo, se
        # generan localmente") -- listarlos aqui hace que git-auto-commit
        # falle con "pathspec did not match any files" (exit 128) porque
        # git rechaza un `add` sobre un pathspec que solo matchea archivos
        # ignorados. Bug real encontrado en el primer run end-to-end
        # (29101919310) de este workflow.
        uses: stefanzweifel/git-auto-commit-action@v5
        with:
          commit_message: "data: metricas actualizadas — pipeline automatico ${{ github.run_id }}"
          file_pattern: "datos/modelos/metricas.json"
          commit_author: "github-actions[bot] <github-actions[bot]@users.noreply.github.com>"
