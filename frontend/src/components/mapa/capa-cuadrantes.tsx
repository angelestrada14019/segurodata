// TODO(Sprint 2): capa toggle de cuadrantes de policía vía RPC
// `cuadrantes_geojson` (requiere sesión autenticada — RLS). Factory:
// `capaCuadrantes(data, onClick) => new GeoJsonLayer(...)`.
export {};
