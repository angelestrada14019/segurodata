# Desarrollo local y tests. fastembed reemplaza a sentence-transformers en dev
# (mismo modelo MiniLM via ONNX, sin torrente de torch) — EMBEDDINGS_BACKEND=fastembed
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
ruff>=0.5.0
fastembed>=0.3.0
